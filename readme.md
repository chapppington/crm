# CRM System DDD

![Coverage](https://img.shields.io/badge/coverage-87%25-brightgreen)

REST API приложение для управления продажами и клиентами, построенное на принципах Domain-Driven Design. Система поддерживает мультитенантность через организации и ролевую модель доступа.

## 📋 Описание

Приложение предоставляет API для управления CRM-системой с поддержкой:

- 🏢 **Мультитенантность** — работа с несколькими организациями
- 👥 **Управление контактами** — создание и управление клиентскими контактами
- 💼 **Управление сделками** — отслеживание сделок с этапами, статусами и суммами
- ✅ **Задачи** — управление задачами, привязанными к сделкам
- 📊 **Активности** — история действий по сделкам (комментарии, изменения статусов)
- 📈 **Аналитика** — аналитика по продажам и сделкам
- 🔐 **Авторизация** — JWT токены для аутентификации пользователей
- 👤 **Роли и права** — система ролей (owner, admin, manager, member) для управления доступом

## 🛠 Стек технологий

### Backend
- **Python 3.13** — язык программирования
- **FastAPI** — веб-фреймворк для REST API
- **Pydantic** — валидация данных и настройки
- **PostgreSQL** — база данных
- **SQLAlchemy 2.0** — ORM для работы с БД
- **Alembic** — миграции базы данных
- **asyncpg** — асинхронный драйвер PostgreSQL

### Инструменты разработки
- **pytest** — тестирование
- **ruff** — линтер и форматтер
- **isort** — сортировка импортов
- **pre-commit** — хуки для проверки кода
- **faker** — генерация тестовых данных
- **bcrypt** — хеширование паролей
- **authx** — JWT аутентификация

### Архитектурные паттерны
- **DDD (Domain-Driven Design)** — проектирование на основе предметной области
- **CQRS** — разделение команд и запросов через Mediator
- **Repository Pattern** — абстракция работы с данными
- **Dependency Injection** — через punq контейнер

### Инфраструктура
- **Docker** — контейнеризация
- **PostgreSQL** — основная база данных
- **uvicorn** — ASGI сервер

## 🚀 Установка и запуск

1. **Клонируйте репозиторий:**
   ```bash
   git clone <repository-url>
   cd crm
   ```

2. **Настройте переменные окружения:**
   ```bash
   cp .env.example .env
   # Отредактируйте .env при необходимости
   ```

3. **Запустите приложение:**
   ```bash
   make all
   ```

4. **Примените миграции:**
   ```bash
   make migrate
   ```

5. **Проверьте работу:**
   - API документация: http://localhost:8000/api/docs
   - Healthcheck: http://localhost:8000/healthcheck

## 📝 Команды Makefile

### Основные команды

| Команда | Описание |
|---------|----------|
| `make all` | Запуск всех сервисов (приложение + хранилища) |
| `make all-down` | Остановка всех сервисов |
| `make migrate` | Применение миграций (upgrade head) |
| `make migrations` | Создание новой миграции (autogenerate) |
| `make test` | Запуск тестов |

### Управление приложением

| Команда | Описание |
|---------|----------|
| `make app-logs` | Просмотр логов приложения |
| `make app-shell` | Подключение к shell контейнера приложения |
| `make app-down` | Остановка приложения |
| `make app-up` | Запуск приложения |

### Управление хранилищами

| Команда | Описание |
|---------|----------|
| `make storages` | Запуск хранилищ (PostgreSQL) |
| `make storages-down` | Остановка хранилищ |
| `make storages-logs` | Просмотр логов хранилищ |
| `make postgres` | Подключение к PostgreSQL через psql |

### Разработка

| Команда | Описание |
|---------|----------|
| `make precommit` | Запуск pre-commit проверок для всех файлов |

## 🏗 Архитектура

Проект построен на принципах Domain-Driven Design и разделен на четыре основных слоя:

### 1. Domain Layer (`app/domain/`)

**Бизнес-логика и правила предметной области**

```
domain/
├── base/                    # Базовые классы
│   ├── entity.py           # Базовая сущность
│   ├── value_object.py     # Базовый объект-значение
│   ├── exceptions.py       # Доменные исключения
│   └── filters.py          # Базовые фильтры
│
├── users/                   # Контекст пользователей
│   ├── entities/           # Сущности: User
│   ├── value_objects/      # Объекты-значения
│   ├── services/           # Доменные сервисы
│   ├── interfaces/         # Интерфейсы репозиториев
│   └── exceptions/         # Исключения домена
│
├── organizations/           # Контекст организаций
│   ├── entities/           # Сущности: Organization, Member
│   ├── value_objects/      # Объекты-значения
│   ├── services/           # Доменные сервисы
│   ├── interfaces/         # Интерфейсы репозиториев
│   └── exceptions/         # Исключения домена
│
└── sales/                  # Контекст продаж
    ├── entities/           # Сущности: Contact, Deal, Task, Activity
    ├── value_objects/      # Объекты-значения
    ├── services/           # Доменные сервисы
    ├── interfaces/         # Интерфейсы репозиториев
    ├── filters/            # Фильтры для запросов
    └── exceptions/         # Исключения домена
```

**Ключевые сущности:**

- `UserEntity` — пользователь системы с email и паролем
- `OrganizationEntity` — организация (мультитенантность)
- `MemberEntity` — член организации с ролью (owner, admin, manager, member)
- `ContactEntity` — контакт клиента с именем, email и телефоном
- `DealEntity` — сделка с названием, суммой, валютой, статусом и этапом
- `TaskEntity` — задача, привязанная к сделке
- `ActivityEntity` — активность по сделке (комментарии, изменения статусов)

**Value Objects:**

- `UserNameValueObject` — имя пользователя
- `UserEmailValueObject` — email пользователя
- `PasswordValueObject` — пароль (bcrypt hash)
- `OrganizationNameValueObject` — название организации
- `ContactNameValueObject` — имя контакта
- `ContactEmailValueObject` — email контакта
- `ContactPhoneValueObject` — телефон контакта
- `DealTitleValueObject` — название сделки
- `DealAmountValueObject` — сумма сделки (>= 0)
- `CurrencyValueObject` — валюта (USD, EUR, RUB и т.д.)
- `DealStatusValueObject` — статус сделки (new, in_progress, won, lost)
- `DealStageValueObject` — этап сделки (qualification, proposal, negotiation, closed)

### 2. Application Layer (`app/application/`)

**Бизнес-процессы и сценарии использования**

```
application/
├── mediator.py            # CQRS медиатор
├── container.py           # Dependency Injection контейнер
├── users/                 # Контекст пользователей
│   ├── commands/          # Команды (изменение состояния)
│   └── queries/           # Запросы (чтение данных)
├── organizations/         # Контекст организаций
│   ├── commands/          # Команды
│   └── queries/           # Запросы
└── sales/                 # Контекст продаж
    ├── commands/          # Команды
    └── queries/           # Запросы
```

**CQRS через Mediator:**
- **Commands** — операции записи/изменения (CreateContactCommand, CreateDealCommand и т.д.)
- **Queries** — операции чтения (GetContactByIdQuery, GetDealsByOrganizationQuery и т.д.)
- **Mediator** — маршрутизация запросов к соответствующим обработчикам

### 3. Infrastructure Layer (`app/infrastructure/`)

**Технические детали и внешние зависимости**

```
infrastructure/
└── database/              # Работа с базой данных
    ├── models/           # SQLAlchemy модели
    │   ├── users/
    │   ├── organizations/
    │   └── sales/
    ├── repositories/     # Реализации репозиториев
    │   ├── users/
    │   ├── organizations/
    │   └── sales/
    ├── converters/       # Конвертеры Entity ↔ Model
    │   ├── users/
    │   ├── organizations/
    │   └── sales/
    ├── gateways/         # Шлюзы для работы с БД
    │   └── postgres.py   # Database класс с сессиями
    └── migrations/       # Alembic миграции
        └── versions/     # Файлы миграций
```

**Реализации:**
- **Database Gateway** — управление асинхронными сессиями SQLAlchemy (read-write и read-only)
- **SQLAlchemy репозитории** — реализация доменных интерфейсов для PostgreSQL
- **InMemory репозитории** — реализации для тестирования без БД
- **Конвертеры** — преобразование между Entity и Model

### 4. Presentation Layer (`app/presentation/`)

**API endpoints и представление данных**

```
presentation/
└── api/                   # REST API endpoints
    ├── main.py           # Создание FastAPI приложения
    ├── auth.py           # Авторизация (JWT)
    ├── dependencies.py   # Зависимости для эндпоинтов
    ├── exceptions.py     # Обработка исключений
    ├── filters.py        # Фильтры для запросов
    └── v1/               # Версия API
        ├── user/         # Эндпоинты пользователей
        ├── organizations/ # Эндпоинты организаций
        ├── contacts/     # Эндпоинты контактов
        ├── deals/        # Эндпоинты сделок
        │   └── activities/ # Эндпоинты активностей
        ├── tasks/        # Эндпоинты задач
        └── analytics/    # Эндпоинты аналитики
```

### Settings (`app/settings/`)

**Конфигурация приложения**
- Настройки через Pydantic Settings
- Поддержка переменных окружения
- Валидация конфигурации при старте

### Tests (`app/tests/`)

**Тестирование**
- Юнит-тесты доменных сущностей и value objects
- Интеграционные тесты приложения (commands/queries)
- API тесты (presentation/api/)
- InMemory репозитории для изоляции тестов

## 📚 API Документация

После запуска доступна по адресу: **http://localhost:8000/api/docs**

### 🔐 Авторизация

API использует JWT токены для авторизации:
- Токены хранятся в cookies
- Access token для коротких операций
- Refresh token для обновления access token
- Все эндпоинты (кроме регистрации и логина) требуют аутентификации

### 📡 Основные эндпоинты

#### User (Пользователи) — **Без авторизации**
- `POST /api/v1/user/register` — регистрация нового пользователя
- `POST /api/v1/user/login` — аутентификация и получение токенов
- `POST /api/v1/user/token/refresh` — обновление access token

#### Organizations (Организации) — **Требует JWT токен**
- `POST /api/v1/organizations` — создание организации
- `GET /api/v1/organizations/{organization_id}` — получение по ID
- `GET /api/v1/organizations` — список организаций пользователя
- `POST /api/v1/organizations/{organization_id}/members` — добавление члена организации
- `GET /api/v1/organizations/{organization_id}/members` — список членов организации

#### Contacts (Контакты) — **Требует JWT токен**
- `POST /api/v1/contacts` — создание контакта
- `GET /api/v1/contacts/{contact_id}` — получение по ID
- `GET /api/v1/contacts` — список контактов с фильтрацией
- `PATCH /api/v1/contacts/{contact_id}` — обновление контакта
- `DELETE /api/v1/contacts/{contact_id}` — удаление контакта

#### Deals (Сделки) — **Требует JWT токен**
- `POST /api/v1/deals` — создание сделки
- `GET /api/v1/deals/{deal_id}` — получение по ID
- `GET /api/v1/deals` — список сделок с фильтрацией
- `PATCH /api/v1/deals/{deal_id}` — обновление сделки
- `DELETE /api/v1/deals/{deal_id}` — удаление сделки
- `POST /api/v1/deals/{deal_id}/activities` — создание активности (комментарий)
- `GET /api/v1/deals/{deal_id}/activities` — список активностей по сделке

#### Tasks (Задачи) — **Требует JWT токен**
- `POST /api/v1/tasks` — создание задачи
- `GET /api/v1/tasks/{task_id}` — получение по ID
- `GET /api/v1/tasks` — список задач с фильтрацией
- `PATCH /api/v1/tasks/{task_id}` — обновление задачи
- `DELETE /api/v1/tasks/{task_id}` — удаление задачи

#### Analytics (Аналитика) — **Требует JWT токен**
- `GET /api/v1/analytics/deals` — аналитика по сделкам
- `GET /api/v1/analytics/revenue` — аналитика по выручке

### 📋 Формат ответов API

Все ответы API возвращаются в едином формате `ApiResponse`:

- `data` — данные ответа
- `meta` — метаинформация
- `errors` — список ошибок (если есть)

```json
{
  "data": {},
  "meta": {},
  "errors": []
}
```

**Примеры ответов:**

**Успешный ответ (200/201):**
```json
{
  "data": {
    "oid": "uuid",
    "name": "Название"
  },
  "meta": {},
  "errors": []
}
```

**Ошибка валидации (400):**
```json
{
  "data": {},
  "meta": {},
  "errors": [
    {"message": "Описание ошибки"}
  ]
}
```

**Ошибка авторизации (401):**
```json
{
  "data": {},
  "meta": {},
  "errors": [
    {"message": "Authentication required"}
  ]
}
```

## 🗄 База данных

### Схема данных

Система использует PostgreSQL с следующими основными таблицами:

- **users** — пользователи системы
- **organizations** — организации (мультитенантность)
- **organization_members** — члены организаций с ролями
- **contacts** — контакты клиентов
- **deals** — сделки
- **tasks** — задачи, привязанные к сделкам
- **activities** — история активностей по сделкам

Подробная схема базы данных описана в файле `database.dbml`.

### Миграции

Миграции выполняются через Alembic:

```bash
# Создание новой миграции
make migrations

# Применение миграций
make migrate
```

## 🧪 Тестирование

### Запуск тестов

```bash
# Все тесты
make test
```

### Структура тестов

- **Domain tests** (`app/tests/domain/`) — тесты доменных сущностей и value objects
- **Application tests** (`app/tests/application/`) — тесты команд и запросов
- **API tests** (`app/tests/presentation/api/`) — интеграционные тесты API

## 🔒 Безопасность

- Пароли хешируются с помощью bcrypt
- JWT токены для аутентификации
- Ролевая модель доступа (owner, admin, manager, member)
- Мультитенантность — пользователи имеют доступ только к своим организациям
- Валидация данных на уровне домена и API