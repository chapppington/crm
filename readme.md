# CRM System - Multi-tenant Backend

![Coverage](https://img.shields.io/badge/coverage-87%25-brightgreen)

Backend-сервис мини-CRM с поддержкой мультитенантности, построенный на принципах Domain-Driven Design. Система предоставляет версионированное JSON API для управления организациями, пользователями, контактами, сделками, задачами и аналитикой.

## ✅ Соответствие требованиям задания

### Обязательные требования

- ✅ **Python 3.10+** — используется Python 3.13
- ✅ **FastAPI (async)** — полностью асинхронный API
- ✅ **SQLAlchemy + Alembic** — SQLAlchemy 2.0 с миграциями через Alembic
- ✅ **PostgreSQL** — основная база данных
- ✅ **JWT-аутентификация** — access/refresh токены через authx
- ✅ **Type hints** — полная типизация во всех слоях приложения
- ✅ **Версионированное API** — `/api/v1/...`
- ✅ **Миграции** — Alembic миграции с автогенерацией
- ✅ **Тесты** — unit и интеграционные тесты (покрытие 87%)
- ✅ **Документация** — Swagger UI по адресу `/api/docs`

### Плюсы (дополнительно реализовано)

- ✅ **Docker / docker-compose** — полная контейнеризация
- ✅ **Pydantic Settings** — конфигурация через переменные окружения
- ✅ **Многослойная архитектура** — api / application / domain / infrastructure
- ✅ **CQRS** — разделение команд и запросов через Mediator
- ✅ **Repository Pattern** — абстракция работы с данными
- ✅ **DDD** — Domain-Driven Design с доменными сущностями и value objects

### Модель данных

Все требуемые сущности реализованы:

- ✅ **Organization** — id, name, created_at
- ✅ **User** — id, email (уникальный), hashed_password, name, created_at
- ✅ **OrganizationMember** — id, organization_id, user_id, role (owner/admin/manager/member), уникальность пары
- ✅ **Contact** — id, organization_id, owner_id, name, email, phone, created_at
- ✅ **Deal** — id, organization_id, contact_id, owner_id, title, amount, currency, status, stage, created_at, updated_at
- ✅ **Task** — id, deal_id, title, description, due_date, is_done, created_at
- ✅ **Activity** — id, deal_id, author_id, type, payload (JSONB), created_at

### Бизнес-правила

Все правила реализованы и покрыты тестами:

- ✅ **Multi-tenant** — работа в контексте организации через заголовок X-Organization-Id
- ✅ **Роли и права** — owner/admin/manager/member с соответствующими ограничениями
- ✅ **Проверки доступа** — 403/404 для чужой организации
- ✅ **Правила по сделкам**:
  - Нельзя закрыть сделку со статусом won, если amount <= 0
  - Автоматическое создание Activity при смене статуса/стадии
  - Запрет отката стадии назад для member (разрешено для admin/owner)
- ✅ **Правила по контактам** — нельзя удалить контакт с активными сделками
- ✅ **Правила по задачам** — нельзя создать задачу для чужой сделки (member), due_date не в прошлом
- ✅ **Организационный контекст** — проверка принадлежности сущностей к организации

### Аналитика

- ✅ **Сводка по сделкам** — `/api/v1/analytics/deals` (количество по статусам, суммы, средний amount, новые сделки за период)
- ✅ **Воронка продаж** — `/api/v1/analytics/funnel` (количество по стадиям, конверсия между стадиями)

### API эндпоинты

Все требуемые эндпоинты реализованы:

- ✅ **Авторизация** — `/api/v1/user/register`, `/api/v1/user/login`, `/api/v1/user/token/refresh`
- ✅ **Организации** — `/api/v1/organizations` (CRUD + members)
- ✅ **Контакты** — `/api/v1/contacts` (CRUD с фильтрацией)
- ✅ **Сделки** — `/api/v1/deals` (CRUD с фильтрацией и сортировкой)
- ✅ **Задачи** — `/api/v1/tasks` (CRUD с фильтрацией)
- ✅ **Активности** — `/api/v1/deals/{deal_id}/activities` (GET, POST для комментариев)
- ✅ **Аналитика** — `/api/v1/analytics/deals`, `/api/v1/analytics/funnel`

### Качество кода

- ✅ **Typed Python** — полная типизация во всех публичных интерфейсах
- ✅ **Обработка ошибок** — 400 (валидация), 401 (неавторизован), 403 (нет прав), 404 (не найдено), 409 (конфликт)
- ✅ **Линтер** — ruff + isort с pre-commit хуками
- ✅ **Тесты** — unit-тесты бизнес-логики и интеграционные тесты API с фикстурами

## 🛠 Технологический стек

- **Python 3.13** + **FastAPI** (async)
- **PostgreSQL** + **SQLAlchemy 2.0** + **Alembic**
- **JWT** (authx) + **bcrypt**
- **Pydantic Settings** для конфигурации
- **pytest** для тестирования
- **Docker** + **docker-compose**

## 🏗 Архитектура

Проект разделен на четыре слоя:

1. **Domain** (`app/domain/`) — бизнес-логика, сущности, value objects, доменные сервисы
2. **Application** (`app/application/`) — use cases через CQRS (Commands/Queries) и Mediator
3. **Infrastructure** (`app/infrastructure/`) — SQLAlchemy модели, репозитории, конвертеры, миграции
4. **Presentation** (`app/presentation/`) — FastAPI роуты, схемы запросов/ответов, обработка ошибок

## 🚀 Быстрый старт

```bash
# Запуск всех сервисов
make all

# Применение миграций
make migrate

# Запуск тестов
make test
```

API документация: **http://localhost:8000/api/docs**

## 📝 Основные команды

| Команда | Описание |
|---------|----------|
| `make all` | Запуск приложения + PostgreSQL |
| `make migrate` | Применение миграций |
| `make test` | Запуск всех тестов |
| `make migrations` | Создание новой миграции |

## 🧪 Тестирование

- **Unit-тесты** — доменные сущности, value objects, бизнес-правила
- **Интеграционные тесты** — полные сценарии через API (регистрация → организация → контакты → сделки → задачи → аналитика)
- **Покрытие** — 87% кода покрыто тестами

## 📚 Документация

Полная документация API доступна после запуска по адресу: **http://localhost:8000/api/docs**

Все эндпоинты требуют JWT токен (кроме регистрации и логина). Токены передаются через cookies или заголовок Authorization.